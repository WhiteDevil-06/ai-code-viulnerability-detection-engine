# Data Layout

- `raw/`: source datasets or links to external source archives
- `interim/`: extracted, uncleaned subsets
- `processed/`: cleaned datasets ready for modeling

The CVEfixes archive remains outside these folders because it is a large,
ignored source dataset.
